import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';


Widget greyLineSeparator() {
  // creates chart widget of one Attribute with name as heading
  return Container(
    color: Colors.grey,
    height: 1,
  );
}